{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell opens a window and a native folder picker. The page it hosts may ask for that picker, nothing else.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default", "dialog:default", "allow-pick-repository"]
}
