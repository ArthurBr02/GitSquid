{
  "name": "gitia-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Native desktop shell for gitia.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
